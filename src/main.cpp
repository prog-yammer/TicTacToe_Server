#include "web/server.h"
#include "web/common/tools.h"

int main()
{
    std::string qew = "-1 0 qoidwj";
    auto m = getMessage(qew);
    std::cout << "";
//    Server server(std::thread::hardware_concurrency(), 8080);
//    server.start();

    return 0;
}